<div class="container">
  <h1>Add Review</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit 'Submit', class: 'btn btn-info mt-2' %>
  <% end %>
</div>
